apiVersion: v1
kind: Service
metadata:
  name: {{ include "ovo.fullname" . }}
  labels:
    {{- include "ovo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ovo.selectorLabels" . | nindent 4 }}
