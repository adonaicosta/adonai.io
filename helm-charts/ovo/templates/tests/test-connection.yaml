apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ovo.fullname" . }}-test-connection"
  labels:
    {{- include "ovo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ovo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
