apiVersion: v1
kind: Service
metadata:
  name: {{ include "pombo.fullname" . }}
  labels:
    {{- include "pombo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pombo.selectorLabels" . | nindent 4 }}
